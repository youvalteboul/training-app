{% extends 'base.html.twig' %}

{% block body %}
    {#{% for tweet in tweets|slice(1, 10) %}#}
        {#{% embed "tweet/card.html.twig" with { tweet: tweet} only %}{% endembed %}#}
    {#{% endfor %}#}

    <form method="post">
        <div class="row">
            <br>
            <br>
            <div class="col-md-3">
                {{ form(form) }}
                <button type="submit" class="btn btn-default">Ajouter</button>
            </div>
        </div>
    </form>
{% endblock %}
